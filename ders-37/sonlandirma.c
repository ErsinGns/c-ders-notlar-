#include <stdio.h>
#include <stdlib.h>



int main(int argc, char const *argv[])
{
    //normal termination = exit() // dosyalar kaydedili r 0 başarı başkası başarısız
    //abnormal termination = abort() // edilmiyebilri
    // exit(0); //başarılı olduğu için sonlandır
    //exit(1); //başarısız olduğu için sonlandır
    // atexit(&function) exit(1) denilince atexite atılmış olan function çalışır ve kapanır
    // atexit atanan fonksyonları ters sırada çağırır sondan başa


    



    return 0;
}
